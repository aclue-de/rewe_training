{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "{ f=$(node -e \"const j=JSON.parse(require('fs').readFileSync(0,'utf8'));process.stdout.write((j.tool_input&&j.tool_input.file_path)||(j.tool_response&&j.tool_response.filePath)||'');\"); case \"$f\" in *.java|*pom.xml) ./mvnw -q spotless:apply ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  },
  "permissions": {
    "deny": [
      "Bash(git push*)"
    ]
  }
}
