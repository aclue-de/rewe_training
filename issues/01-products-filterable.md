# Products should be filterable

Right now `/api/products` just dumps the whole list. Ops asked whether we can
filter it — for the deposit machine they mainly care about crates and one-way
bottles. Can you add that?
